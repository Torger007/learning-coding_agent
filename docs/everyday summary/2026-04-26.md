# 2026-04-26 每日总结

## 今日目标

将项目从 Kimi 专用配置调整为通用 AI Provider 配置，并接入火山引擎 Coding Plan 的 OpenAI-compatible 协议，解决 API Key、接口地址和模型配置带来的运行问题。

## 完成内容

- 统一后端 AI 配置命名：
  - 新增 `AI_API_KEY`。
  - 新增 `AI_BASE_URL`。
  - 新增 `AI_MODEL`。
  - 保留旧 `KIMI_*` 变量作为兼容兜底。
- 修复旧配置默认值干扰问题：
  - 移除 `KIMI_BASE_URL` 和 `KIMI_MODEL` 的默认值。
  - 避免默认 Kimi 地址覆盖火山 Coding Plan 地址。
- 明确项目协议类型：
  - 当前后端使用 OpenAI Chat Completions 协议。
  - 火山 Coding Plan 应使用 `https://ark.cn-beijing.volces.com/api/coding/v3`。
  - 不应使用普通方舟地址 `https://ark.cn-beijing.volces.com/api/v3`，避免产生额外费用。
  - 不应使用 Anthropic 兼容地址 `https://ark.cn-beijing.volces.com/api/coding`，除非后端改成 Anthropic Messages 协议。
- 修复 AI 请求 URL 拼接逻辑：
  - 支持填写 base URL：`https://ark.cn-beijing.volces.com/api/coding/v3`。
  - 支持填写完整 endpoint：`https://ark.cn-beijing.volces.com/api/coding/v3/chat/completions`。
  - 避免重复拼接 `/chat/completions`。
- 优化错误排查信息：
  - 上游 AI API 报错时返回实际请求 URL。
  - 上游 AI API 报错时返回当前使用的 model。
  - 方便定位 401、404 等配置问题。
- 调整前端 API Key 逻辑：
  - 前端 API Key 改为可选。
  - 如果后端已配置 `AI_API_KEY`，前端可以直接发送指令。
  - 只有用户在前端填写 API Key 时，才会覆盖后端环境变量。
- 更新前端设置弹窗文案：
  - 将 Kimi 文案改成通用 AI API Key。
  - 指引用户前往火山方舟控制台获取 Coding Plan 可用 Key。
- 更新文档：
  - `README.md`
  - `docs/api-key-guide.md`
  - `docs/mvp-summary.md`
  - `backend/render.yaml`
- 增加 AI 适配器测试：
  - 验证 request-scoped API Key 优先级。
  - 验证 base URL 自动拼接 chat completions endpoint。
  - 验证完整 endpoint 不会重复拼接。
- 已提交并上传到 GitHub 分支：
  - `codex/finish-mvp-upload`
  - commit: `5acfd96 Support AI provider configuration`

## 验证结果

在 conda 环境 `LC` 中完成验证：

```powershell
conda activate LC
pytest tests/unit
```

结果：`13 passed`

```powershell
conda activate LC
npx tsc -b
```

结果：通过

```powershell
conda activate LC
npm run build
```

结果：通过

## 当前状态

项目已经完成从 Kimi 专用接入到通用 AI Provider 接入的迁移。当前推荐配置为：

```env
AI_API_KEY=你的火山CodingPlan API Key
AI_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v3
AI_MODEL=doubao-seed-code
```

当前项目属于火山 Coding Plan 的 OpenAI-compatible 协议接入方式：

```text
https://ark.cn-beijing.volces.com/api/coding/v3
```

## 后续建议

- 使用真实火山 Coding Plan API Key 完成一次端到端联调。
- 如果继续遇到 401，优先检查 API Key 类型、权限和前端是否覆盖了后端配置。
- 如果继续遇到 404，优先检查实际请求 URL 和 `AI_MODEL` 是否与火山控制台一致。
- 将 `codex/finish-mvp-upload` 合并到 `master`，标记 MVP 阶段完成。
- 后续可继续推进部署、Electron 桌面壳和会话持久化。
