# 2026-04-25 每日总结

## 今日目标

根据 `docs/mvp-plan.md` 对项目进行 MVP 收尾，补齐 P0 主流程、验证可运行状态，并上传到 GitHub。

## 完成内容

- 完成前端双栏学习界面：编程助手 + 理解助手。
- 补齐代码选中 Ask 流程：
  - Monaco Editor 中选中代码后显示 Ask 浮层。
  - 点击 Ask 后将代码片段带入理解助手。
  - 理解助手输入框自动填充选中代码上下文。
- 完成 WebSocket 流式对话联调逻辑：
  - 编程通道支持流式生成代码。
  - 理解通道支持围绕代码片段继续提问。
  - 前端 WebSocket 增加待发送消息队列，避免连接未就绪时丢消息。
- 完成 API Key MVP 配置流程：
  - 前端弹窗输入 AI API Key。
  - 请求级 API Key 可传给后端。
  - 后端支持环境变量兜底。
- 强化后端沙箱安全：
  - 使用 AST + 正则进行危险代码静态扫描。
  - 禁止危险模块与危险函数。
  - Docker 执行参数补齐只读、无网络、资源限制和 `no-new-privileges`。
  - 移除本地 Python 执行兜底，确保执行必须走 Docker 沙箱。
- 补齐文档：
  - `docs/api-key-guide.md`
  - `docs/mvp-summary.md`
  - 更新根目录 `README.md`
- 修复 `.gitignore` 误忽略 `frontend/src/lib/utils.ts` 的问题。
- 已提交并上传到 GitHub 分支：
  - `codex/finish-mvp-upload`
  - commit: `0e34267 Finish MVP implementation`

## 验证结果

在 conda 环境 `LC` 中完成验证：

```powershell
conda activate LC
pytest tests/unit
```

结果：`11 passed`

```powershell
conda activate LC
npx tsc -b
```

结果：通过

```powershell
conda activate LC
npm run build
```

结果：通过

## 当前状态

MVP P0 主流程已经可以作为阶段性交付：

1. 用户在编程通道提问。
2. AI 流式返回代码。
3. 用户选中代码片段并点击 Ask。
4. 理解通道携带代码上下文继续问答。
5. 后端提供 REST API、WebSocket、AI 适配和 Docker 沙箱执行能力。

## 后续建议

- 手动完成一次端到端真实 API Key 联调。
- 部署后端到 Render，并配置前端生产环境变量。
- 部署前端到 Vercel。
- 后续再推进 P2：Electron 桌面壳、多模型切换、SQLite 历史记录。
