services:
  - type: web
    name: learning-coding-agent-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: KIMI_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: "*"
